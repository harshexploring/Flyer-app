// ============================================================
// Supabase config — COPY this file to `config/supabase.js` and fill
// in your two values from the Supabase dashboard:
//   Project Settings → API → Project URL   and   anon / public key
//
// The anon key is designed to live in the browser (it's safe to ship);
// Row-Level Security in schema.sql is what actually protects the data.
// The game still works fully as a guest if these are left blank.
// ============================================================

export const SUPABASE_URL = 'https://YOUR-PROJECT.supabase.co';
export const SUPABASE_ANON_KEY = 'YOUR-ANON-PUBLIC-KEY';
